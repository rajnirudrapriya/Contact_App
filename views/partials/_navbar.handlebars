<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Call Me Maybe - Navbar</title>
  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      margin: 0;
      background: linear-gradient(120deg, #f6d365 0%, #fda085 100%);
    }

    .navbar {
      background-color: #fff3e0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .navbar .logo {
      font-size: 1.5rem;
      font-weight: bold;
      color: #e85c50;
    }

    .nav-links {
      display: flex;
      gap: 1.5rem;
    }

    .nav-links a {
      text-decoration: none;
      color: #333;
      font-size: 1rem;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      transition: background 0.3s ease;
    }

    .nav-links a:hover {
      background-color: #ffe0cc;
    }
  </style>
</head>
<body>

  <nav class="navbar">
    <div class="logo">Call Me Maybe</div>
    <div class="nav-links">
      <a href="/">🏠 Home</a>
      <a href="/api/addContact">➕ Add a Contact</a>
      <a href="/api/fetchContact">📇 All Contacts</a>
    </div>
  </nav>

</body>
</html>
